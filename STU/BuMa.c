#include <stdio.h>

int main() {
    unsigned char a = 100, b = 100;
    char x = 100, y = 100;
    printf("a + b = %d\n", (unsigned char)(a + b));
    printf("x + y = %d\n", (char)(x + y));
    return 0;
}